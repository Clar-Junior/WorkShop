#include <stdio.h>
#include <string.h>

// ISNULL (NÃO FAZ PARTE DA LINGUAGEM)
// Teste para descobrir se há caracteres na variável
// verifica se na posição[0] é igual a '\0', se for = vazio, senão = há caracteres.

/*int main()
{
    char nome[10] = {""};

    if (nome[0] == '\0')
    {
        printf("vazio\n");
    }
    else
        printf("ha texto");

    return 0; // pode ser retirado sem problema
}
*/